// LINE's URI action opens this same-origin page in its in-app browser.
// Reuse the existing parent session and server authorization; never infer identity from LINE.
export { default } from '../page';
